mlx-lm>=0.21.0
# Vision: lets the assistant see screenshots instead of guessing from page text.
mlx-vlm>=0.6.0
# Reads a screenshot's text before the VLM is woken (symbio/ocr.py). macOS only;
# without it see_screen goes straight to the vision model.
pyobjc-framework-Vision>=10.0; sys_platform == "darwin"
transformers>=4.40.0
torch
safetensors>=0.4.0
pyyaml>=6.0
playwright>=1.45.0
PyAutoGUI>=0.9.54
Pillow>=10.0.0
python-telegram-bot>=20.0

# CUDA backend (symbio/backend.py, symbio/cuda_lora.py). Not installed by
# default and NEVER EXECUTED — written on a Mac with no NVIDIA device.
# On a CUDA box install torch for your CUDA version from pytorch.org, then:
#   pip install peft accelerate bitsandbytes datasets
